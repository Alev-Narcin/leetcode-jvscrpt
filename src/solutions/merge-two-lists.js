const MergeTwoLists = (props) => {
    const list = {val: undefined, next: undefined}
    let val = list.val
    let next = list.next

    val = (val === undefined ? 0 : val)
    next = (next === undefined ? null : next)



}
export default MergeTwoLists;